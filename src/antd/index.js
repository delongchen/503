import Vue from 'vue'

import {
  Button,
  Card,
  PageHeader,
  Icon,
  Tag,
  Form,
  Modal
} from 'ant-design-vue'

Vue.use(Button)
Vue.use(Card)
Vue.use(PageHeader)
Vue.use(Icon)
Vue.use(Tag)
Vue.use(Form)
Vue.use(Modal)

import Dialog from "@/components/Dialog";

Vue.use(Dialog)
